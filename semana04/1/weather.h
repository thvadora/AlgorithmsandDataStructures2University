#ifndef _WEATHER_H
#define _WEATHER_H

#include <stdbool.h>

int min_temp_hist(tclimate(a));

void the_maximum(tclimate(a), int output[YEARS]);

void precip_hist(tclimate(a),t_month output[YEARS]);

#endif

